[user]
	name = colpshift
	email = 39013278+colpshift@users.noreply.github.com
	signingkey = /home/colps/.ssh/id_ed25519
[gpg]
	program = /bin/gpg
	format = ssh
[credential]
	helper = store --file ~/.git-credentials
[core]
	editor = nvim
	pager = diff-so-fancy | less --tabs=4 -RF
[interactive]
	diffFilter = diff-so-fancy --patch
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = 11
	frag = magenta bold
	func = 146 bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

